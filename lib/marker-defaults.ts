import type { ArrowDirection, ArrowPosition } from "../types/formfeed";

/** DB/API 호환용 고정값 (UI에서는 화살표 미사용) */
export const MARKER_ARROW_POSITION: ArrowPosition = "top-center";
export const MARKER_ARROW_DIRECTION: ArrowDirection = "right";
